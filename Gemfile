source 'https://rubygems.org'

gem 'dotenv'
gem 't'

group :daemon do
  gem 'daemons', '~> 1.0'
end

group :test do
  gem 'assert',       "~> 2.8"
  gem 'assert-mocha', "~> 1.1"
end

group :development, :test do
  gem 'pry-nav'
end
